<% provide(:title, 'Edit User') %>
<% provide(:button_text, 'Save Changes') %>

<div class="container">
  <div class="row">
    <div class="col-sm-4">
      <h2>Update Your Profile</h2>

      <%= render 'form' %>

      <div style="text-align: center" class="gravatar_edit">
        <%= gravatar_for @user %>
        <a href="http://gravatar.com/emails" target="blank" rel="noopener">change</a>

    </div>
  </div>
</div>
